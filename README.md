# Installation

Nedd to install : 
- pdm (https://pdm-project.org/latest/)
- python >= 3.11

Then run the following command to install the dependencies:
```bash
pdm install
```

# Usage
```bash
pdm run tasks
```